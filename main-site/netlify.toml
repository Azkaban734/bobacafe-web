[build]
  # Build schedule-app and copy into main-site so /schedule-app/ is deployed
  command = "bash build.sh"
  publish = "."

[[redirects]]
  from = "/schedules/*"
  to = "https://shiftfinder.netlify.app/:splat"
  status = 200
  force = true